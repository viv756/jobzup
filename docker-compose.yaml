version: "3.9"
services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env 
  client:
    build: ./client
    ports:
      - "5173:5173"
    env_file:
      - ./client/.env  
    stdin_open: true
    tty: true
    depends_on:
      - server
